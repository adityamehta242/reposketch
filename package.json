{
  "name": "reposketch",
  "version": "1.0.0",
  "description": "a tool that takes a GitHub repository, analyzes its project file structure, and outputs it to a text file",
  "license": "ISC",
  "author": "aditya-mehta",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "shelljs": "^0.10.0"
  }
}
